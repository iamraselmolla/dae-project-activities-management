import React from 'react';

const Distribution = () => {
    return (
        <div className="h-screen font-bold text-center text-2xl">
            This is the page for distributions
        </div>
    );
};

export default Distribution;